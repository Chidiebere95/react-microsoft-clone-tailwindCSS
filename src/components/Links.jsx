import React from 'react'

const Links = ({item}) => {
    return (
        <div>
              <p className="text-xs font-light  mb-3">{item}</p>
                    
        </div>
    )
}

export default Links
